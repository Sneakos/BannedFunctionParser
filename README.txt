go.exe expects a file name as an argument. It will create two files (inputfile_Bad.c and inputfile_Good.c)

You can run FileSplit.py to create two files for every file in the "s0" folders, 
just make sure the python file is located in the "s0" folders parent folder